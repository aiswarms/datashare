# Journal d'usage IA

L'IA (Claude Code) a été sollicitée uniquement sur la phase de tests et de qualité du projet, une fois le code applicatif entièrement développé.

| Date | Étape / tâche | Outil IA | Prompt (résumé) | Ce que l'IA a proposé | Décision | Vérification & conclusion |
|------|--------------|----------|-----------------|----------------------|----------|--------------------------|
| 2026-06-10 | Configuration couverture Vitest | Claude Code | Ajouter les reporters `lcov` et `html` dans `vite.config.ts` pour générer un rapport de couverture exploitable | Modification de `vite.config.ts` : ajout de `reporter: ['text', 'lcov', 'html']` et `reportsDirectory: './coverage'` | Gardé | Couverture générée correctement, rapport HTML accessible dans `frontend/coverage/index.html` |
| 2026-06-10 | Installation Cypress et scripts npm | Claude Code | Installer Cypress 15, ajouter les scripts `cy:open` et `cy:run` dans `package.json` | Ajout de `cypress` en devDependency, scripts `cy:open` et `cy:run` dans `package.json`, création de `cypress.config.ts` avec `baseUrl`, `specPattern`, `video: false` | Gardé | Configuration fonctionnelle après ajustement du `defaultCommandTimeout` à 8000ms |
| 2026-06-10 | Commandes personnalisées Cypress | Claude Code | Créer des commandes réutilisables pour l'inscription, la connexion et l'upload via l'API, à utiliser dans tous les fichiers de tests E2E | Fichier `cypress/support/commands.ts` avec `cy.apiRegister()`, `cy.apiLogin()` (stockage JWT dans localStorage) et `cy.apiUpload()` utilisant `cy.window().then(win => win.fetch(...))` pour les uploads FormData | Gardé après correction | `cy.request()` ne supportant pas FormData, l'approche `win.fetch()` était nécessaire ; les types TypeScript ont été ajustés |
| 2026-06-10 | Scénarios E2E — authentification (01-auth.cy.ts) | Claude Code | Écrire 6 scénarios Cypress couvrant inscription valide, email déjà utilisé, mot de passe trop court, connexion valide, mauvais mot de passe, déconnexion | 6 scénarios complets avec sélecteurs, assertions sur les toasts et redirections | Gardé après corrections | Ajustements des libellés de boutons et des messages de toast pour correspondre au DOM réel |
| 2026-06-10 | Scénarios E2E — upload authentifié (02-upload.cy.ts) | Claude Code | Écrire 5 scénarios : upload simple, upload avec mot de passe, upload avec tags, upload avec expiration, type de fichier interdit | 5 scénarios avec `cy.selectFile()` pour la sélection de fichier, vérification du lien de téléchargement généré, et test d'erreur pour extension `.exe` | Corrigé | Le code d'erreur utilisé était `FORBIDDEN_EXT` au lieu de `FORBIDDEN_FILE_TYPE` retourné par le controller ; corrigé après lecture de `UploadController.php` |
| 2026-06-10 | Scénarios E2E — upload anonyme (03-anonymous-upload.cy.ts) | Claude Code | Écrire 3 scénarios : upload anonyme réussi, upload anonyme avec mot de passe, redirection d'un utilisateur connecté | 3 scénarios sans authentification préalable, vérification du lien généré | Gardé | Scénarios conformes au comportement de l'API |
| 2026-06-10 | Scénarios E2E — téléchargement (04-download.cy.ts) | Claude Code | Écrire 6 scénarios couvrant l'affichage des métadonnées, le téléchargement libre, la protection par mot de passe (correct et incorrect), et le lien invalide | 6 scénarios avec setup via `cy.apiUpload()`, vérification du champ mot de passe conditionnel, assertion sur les messages d'erreur | Gardé après ajustements | Sélecteurs CSS ajustés pour correspondre aux composants Chakra UI réels |
| 2026-06-10 | Scénarios E2E — historique (05-history.cy.ts) | Claude Code | Écrire 6 scénarios couvrant l'affichage de l'historique, le filtre par tag, la suppression avec confirmation, l'annulation de suppression, l'état vide, et le filtre par statut | 6 scénarios avec navigation vers `/my-space`, interaction avec les filtres et les dialogues de confirmation | Gardé après ajustements | Attributs `data-testid` vérifiés dans les composants React ; un sélecteur de filtre corrigé |
| 2026-06-11 | Pipeline CI/CD GitHub Actions | Claude Code | Créer un workflow GitHub Actions avec deux jobs (frontend et backend), service PostgreSQL, génération des clés JWT, exécution des tests avec couverture et upload des artifacts | Fichier `.github/workflows/ci.yml` avec jobs `frontend` (npm ci → vitest → coverage) et `backend` (composer → JWT keys → migrations → phpunit) | Corrigé (×3) | **Correction 1** : `POSTGRES_DB` mis à `datashare_test` (Symfony ajoute le suffixe `_test` en mode test via `doctrine.yaml`). **Correction 2** : ajout de l'étape `lexik:jwt:generate-keypair` car les clés ne sont pas versionnées. **Correction 3** : ajout de `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` pour supprimer les warnings de dépréciation Node.js 20 dans les actions GitHub |
| 2026-06-11 | Scripts de performance k6 | Claude Code | Créer deux scripts k6 : un pour l'upload (montée en charge 1→20 VU) et un pour le download (20 VU constants), avec seuils de performance définis | `perf/k6-upload.js` avec scénario de rampe et threshold p95 < 3000ms ; `perf/k6-download.js` avec `setup()` qui uploade un fichier fixture et threshold p95 < 500ms | Gardé | Scripts syntaxiquement corrects et conformes à l'API k6 ; non exécutés (k6 non installé dans l'environnement de dev) |
